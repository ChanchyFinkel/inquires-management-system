// Single-instance, single-environment exam scope — a full environment.ts/environment.prod.ts
// swap would be speculative for a project this size. Change this if the API port changes.
export const API_BASE_URL = 'http://localhost:5120/api';
